스택

정의: 원소의 삽입, 삭제가 한쪽 끝( >>> 탑 )에서만 이뤄지도록 제한한 유한 순서 리스트

스택의 중점 사항
    원소의 삽입, 삭제가 탑에서만 이뤄진다는 것
    후입 선출

스택 연산
    기본
        push: 삽입
        pop: 삭제
    추가
        delete: 제거
        peek: 추출
        isEmpty: 빈 스택인지 확인

    *** pop VS delete VS peek
        pop: delete + peek

스택 응용
    1. 리스트 순서 뒤집기
    2. 런타임 스택 (각 원소는 ( 지역 변수 + 형식 매개변수 + 복귀 주소 )로 구성)
    3. 재귀

스택 구현
    1. 순차 표현
        장점: 간단하다?
        단점: 만원 처리에 비용이 많이 든다
    2. 연결 표현